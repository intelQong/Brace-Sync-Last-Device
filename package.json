{
  "name": "privacy-sync-free",
  "version": "0.1.0",
  "private": true,
  "description": "End-to-end encrypted browser data sync web app and Cloudflare backend",
  "workspaces": [
    "backend",
    "web",
    "shared"
  ],
  "scripts": {
    "test": "npm run test -w backend && npm run test -w shared",
    "build": "npm run build -w web",
    "lint": "npm run lint -w backend && npm run lint -w shared && npm run lint -w web"
  }
}
