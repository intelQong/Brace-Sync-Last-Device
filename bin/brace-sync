#!/usr/bin/env python3
"""Run the local Brace Sync CLI from a source checkout."""

from pathlib import Path
import sys

ROOT = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(ROOT / "src"))

from brace_sync.cli import main

raise SystemExit(main())
